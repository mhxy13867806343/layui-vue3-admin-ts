# 是否启用本地 Mock 服务（true 时启用 vite-plugin-mock，关闭后端代理）
VITE_USE_MOCK=true

# HTTP 客户端基础地址（开发期通常为 /api，由代理或 Mock 接管；生产期填真实后端域名）
VITE_API_BASE_URL=/api

# Vite 开发服务器端口（仅开发期生效）
VITE_PORT=5173

# 静态资源公共前缀（部署到子路径时改成对应路径，例如 /admin/）
VITE_PUBLIC_PATH=/
